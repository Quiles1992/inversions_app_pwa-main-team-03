{
  "name": "inversions-app-pwa",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "lint": "npm run -w frontend lint && npm run -w backend lint",
    "test": "npm run -w frontend test && npm run -w backend test"
  }
}
